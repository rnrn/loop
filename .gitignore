node_modules/
.DS_Store
*.log

# Исходная методичка мастер-класса — не для публичного доступа.
# Хранится локально у модератора, в репозиторий не попадает.
*.docx
doc.txt

# рабочие снимки модератора, создаваемые через `npm run checkpoint -- save`
checkpoints/before-rollback/
